pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building retailapp_platform'

                sh '''
                    pwd
                    git --version
                '''
            }
        }
    }
}